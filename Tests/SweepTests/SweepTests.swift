import XCTest
@testable import Sweep

final class SweepTests: XCTestCase {
    func testPlaceholder() {
        // Real tests added per service as they are implemented
        XCTAssertTrue(true)
    }
}
